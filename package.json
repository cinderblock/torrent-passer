{
	"name": "torrent-passer",
	"version": "0.4.0",
	"description": "Tiny GUI that opens when you double-click a .torrent file and forwards it to a configurable destination (POST URL or Deluge).",
	"private": true,
	"scripts": {
		"start": "electrobun dev",
		"dev": "electrobun dev --watch",
		"build": "electrobun build",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"deluge-rpc-socket": "1.0.0",
		"electrobun": "1.18.1"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	}
}
